%h1 Import historical data

= form_tag({action: :upload_history}, multipart: true) do
  .form-group
    %label{ for: "data_file" } Data File
    = file_field_tag 'data_file'
    %p.help-block
      Please note that the file
      must be in Excel (
      %u XLSX
      ) format.
  = submit_tag 'Upload', class: "btn btn-primary"
  = link_to "Cancel", root_path, class: "btn btn-primary"